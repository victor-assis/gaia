{
  "name": "@houf/tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "tsc -w tsconfig.json",
    "build": "run-s build:**",
    "build:ts": "tsc -p tsconfig.json",
    "build:package": "copyfiles -u 1 \"./src/package.json\" ../../dist/tools",
    "lint": "eslint",
    "fix": "eslint --fix",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
